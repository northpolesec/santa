// Operator "santa-operator"
resolver: MEMORY

port: 4222
host: 0.0.0.0

# JetStream configuration
jetstream {
  # Storage directory
  store_dir: "/var/lib/nats/jetstream"
  
  # Maximum storage (2GB)
  max_memory_store: 2GB
  max_file_store: 2GB
}
